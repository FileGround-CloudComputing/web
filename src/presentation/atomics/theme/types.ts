export interface Theme {
  colors: {
    background: string;
    darkenBackground: string;
    lightenBackground: string;
    onBackground: string;
    lightShadow: string;
    darkShadow: string;
  };
}
